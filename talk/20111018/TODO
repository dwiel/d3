keyboard shortcuts
enforce rules:
  virtual meters must be computable
    what is computable?
allow multiplier

intigration with plotwatt
  style
  save
  load
  allow switching between readings and disag topo
