allow new computed meters
enforce rules:
  computed meters must be computable
    what is computable?
allow multiplier
help bar on right hand side

intigration with plotwatt
  style
  save
  load
  allow switching between readings and disag topo
