down arrow should travel to cousins when out of siblings
type, autocomplete add node
enforce rules:
  computed meters must be computable
    what is computable?

intigration with plotwatt
  style
  save
  load
  allow switching between readings and disag topo
